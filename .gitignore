# Python
*.pyc
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/

# Django
db.sqlite3
/static/
ttstats/staticfiles/
/media/
*.log

# Environment
# .env.dev is tracked on purpose -- it is the dev template and holds only fake
# credentials. Anything real (a Supabase DATABASE_URL) goes in .env.dev.local,
# which compose.dev.yml loads after it and which is never committed.
.env
.env.dev.local
.env.prod

# Frontend build output (produced by `npm run build:css`; the Docker image
# builds it in the `assets` stage, compose.dev.yml rebuilds it on the fly)
node_modules/
ttstats/pingpong/static/pingpong/css/app.css
ttstats/pingpong/static/pingpong/js/vendor/
ttstats/pingpong/static/pingpong/css/vendor/
ttstats/pingpong/static/pingpong/fonts/

# Docker
docker-compose.override.yml

# IDE
.vscode/
.idea/
*.swp
*.swo

.DS_Store

# Coverage
.coverage
.coverage
htmlcov/
.coverage.*
.codanna
.fastembed_cache
# Playwright
node_modules/
test-results/
playwright-report/
ttstats/e2e.sqlite3
tests/e2e/.auth/

# Local demo dataset for design work (manage.py seed_demo) and the
# screenshots taken from it. Both are regenerable and neither belongs in git.
ttstats/demo.sqlite3
design-screenshots/
